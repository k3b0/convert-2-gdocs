{
  "name": "google-docs-converter",
  "version": "0.3.0",
  "description": "Converts HTML/Markdown to Google Docs API requests",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --reporter verbose",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@vitest/coverage-v8": "^1.2.2",
    "cheerio": "1.0.0-rc.12",
    "vite-plugin-dts": "^4.5.0",
    "vitest": "^1.2.2"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
